import { createStyles } from '../../../../utils/create-styles';

export const styles = createStyles({
  container: {
    display: 'flex',
    width: '100%',
    height: '100%',
  },

  container__center: {
    display: 'flex',
    flexGrow: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
