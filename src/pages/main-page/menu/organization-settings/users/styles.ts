import { createStyles } from '../../../../../utils/create-styles';

export const styles = createStyles({
  container: {
    flexGrow: 1,
    overflowY: 'scroll',
    m: -4,
    pb: 20,
  },

  users__fab: {
    position: 'absolute',
    bottom: 16,
    right: 16,
  },
});
