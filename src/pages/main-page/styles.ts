import { createStyles } from '../../utils/create-styles';

export const styles = createStyles({
  container: {
    display: 'flex',
    width: '100%',
    height: '100%',
  },

  main: {
    display: 'flex',
    width: '100%',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
